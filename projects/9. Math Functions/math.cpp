#include <iostream>
#include <cmath>

int main() {

    double x = 3;
    double y = 10;
    double z;

    // More math functions at: https://cplusplus.com/reference/cmath/
    // z = std::max(x, y);
    // z = std::min(x, y);
    // z = std::sqrt(x);

    // z = std::round(2.6);
    // z = std::ceil(2.6);
    // z = std::floor(2.6);

    // z = std::abs(-2.6);

    z = std::pow(2, 3);

    std::cout << z << '\n';
    return 0;
}